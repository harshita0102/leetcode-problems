#include <stdbool.h>
bool isPalindrome(int x) {
    if (x < 0) {
        return false;
    }

    int n = x;
    long remdiv = 0;

    while (x > 0) {

        int d = x % 10;
        remdiv = remdiv * 10 + d;
        x = x / 10;
    }

    return (remdiv == n);
}
