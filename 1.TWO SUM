
int* twoSum(int* nums, int numsSize, int target, int* returnSize) {
    
    *returnSize = 2;
    
  
    static int result[2]; 

  h
    for (int i = 0; i < numsSize; i++) {
        for (int j = i + 1; j < numsSize; j++) {
            
            if (nums[i] + nums[j] == target) {
                result[0] = i; // Save first index
                result[1] = j; // Save second index
                return result; // Send the answer back to LeetCode
            }
            
        }
    }

   
    *returnSize = 0;
    return NULL;
}
